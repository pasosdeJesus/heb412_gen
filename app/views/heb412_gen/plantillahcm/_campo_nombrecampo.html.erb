<% 
  ab = ::Ability.new 
  v = defined?(vista) ? vista : 
    defined?(@vista) ? @vista :
    defined?(f.object.plantillahcm.vista) ? 
    f.object.plantillahcm.vista :
    defined?(Ability::campos_plantillas['Usuario']) ? 'Usuario' : nil
  if v.nil? || Ability::campos_plantillas[v].nil?
    col = [['','']]
  else
    col = Heb412Gen::PlantillaHelper.campos_vista_formulario(
      v, f.object.plantillahcm ? f.object.plantillahcm.formulario : nil,
      false)
    #col = Ability::campos_plantillas[v][:campos]
  end

  col = col.map(&:to_s).localize(:es).sort.to_a
  puts "OJO campoplantillahcm. col=#{col.to_s}"
%>
<%= f.input :nombrecampo,
  collection: col,
  label: false
%>
