$("#ruta").html("<%= @ruta%>");
$("#nuevo_ruta").val("<%= File.join("arch", @ruta) %>");
$("#nueva_ruta").val("<%= File.join("arch", @ruta) %>");
$("#contenido").html("<%= escape_javascript(render('heb412_gen/sisarch/index_contenido')) %>");
$('#nueva_nombre').val('');
// Buscamos que al crear carpeta nuevo e ingresar presente menus contextuales
heb412_gen_prepara_eventos_comunes(window)
<% if @modhistorial %>
  window.location.pathname = "<%= File.join(
    Rails.configuration.relative_url_root, 
    'sis/arch', @ruta) %>";
<% end %>
