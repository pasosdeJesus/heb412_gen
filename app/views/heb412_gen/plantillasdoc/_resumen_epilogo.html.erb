<% if @registro && @registro.vista %>
  <p>
  En la plantilla puede emplear los campos:
  <blockquote>
    <% ab = ::Ability.new %>
    <% Ability::campos_plantillas[@registro.vista][:campos].each do |c| %>
      [<%= c.upcase %>]
    <% end %>
  </blockquote>
  </p>
<% end %>
