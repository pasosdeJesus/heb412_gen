<% ods =[] 
Dir.chdir(Rails.application.config.x.heb412_ruta) { 
  ods= Dir.glob("**/*.ods") } 
%>

<%= f.input :ruta, 
  collection: ods,
  input_html: {
    class: "tom-select",
    "data-toggle" => 'tooltip',
    "title" => 'Plantilla, debe ser hoja de cálculo (ods)'
  } %>

